[pytest]
testpaths = tests
pythonpath = .
addopts = -v -m "not postgres" --cov=extract --cov=pipeline --cov-report=term-missing
markers =
    postgres: needs a live PostgreSQL, run in CI against a service container
